// Slavnem @2024-02-26
// Tamsayılar Okuma

// Kütüphaneler
#include <stdio.h>

// main
int main(void)
{
    int a;
    int b;
    int c;
    int d;
    int e;
    int f;
    int g;

    puts("Yedi tane tam sayi giriniz:");
    scanf("%d%i%i%i%o%u%x", &a, &b, &c, &d, &e, &f, &g);

    puts("\nOndalik tam sayi olarak goruntulenen girdiler:");
    printf("%d %d %d %d %d %d %d\n", a, b, c, d, e, f, g);
}